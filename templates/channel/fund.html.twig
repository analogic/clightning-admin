{% extends 'base.html.twig' %}

{% block title %}Fund channel {{ id|truncate(15) }}{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}
